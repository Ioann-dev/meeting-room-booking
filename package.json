{
  "name": "meeting-room-booking",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run build -w packages/shared && concurrently -n shared,api,web -c yellow,green,cyan \"npm run dev -w packages/shared\" \"npm run start:dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run build -w packages/shared && npm run typecheck --workspaces --if-present",
    "test": "npm run build -w packages/shared && npm run test --workspaces --if-present",
    "test:integration": "npm run build -w packages/shared && npm run test:e2e -w apps/api",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "prettier": "^3.9.6"
  }
}
