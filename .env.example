# Used by docker-compose.yml to configure the local Postgres container.
# Values must match apps/api/.env's DATABASE_URL/TEST_DATABASE_URL.
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=meeting_room_booking
POSTGRES_PORT=5432
